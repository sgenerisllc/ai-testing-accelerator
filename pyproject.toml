[tool.pytest.ini_options]
testpaths = ["tests"]
norecursedirs = [
    ".git",
    ".venv",
    "venv",
    "demos/05-autonomous-playwright-agent/generated",
]

[tool.black]
line-length = 100
target-version = ["py310"]
